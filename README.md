The website uses AJAX to send the POST request to check the login details.
As no signup form is there so simple implementation is used without any database 
To login use :
  username = vaibhav
  password = password
